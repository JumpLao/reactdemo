// import { fromJS } from 'immutable';
// import { selectSocialAccountDetailPageDomain } from '../selectors';

describe('selectSocialAccountDetailPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
