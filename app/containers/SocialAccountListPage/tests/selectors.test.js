// import { fromJS } from 'immutable';
// import { selectSocialAccountListPageDomain } from '../selectors';

describe('selectSocialAccountListPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
